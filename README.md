Export MONGO_URI configuration variable:

```bash
$ export MONGO_URI="mongodb://localhost:27017/<database>"
```

Run unittests:

```bash
$ python -m unittest
```

Run server locally:

```bash
$ python app.py
```